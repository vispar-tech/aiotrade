"""Entry point for unified exchange clients.

This module is intended as a namespace aggregator or utility for importing
all unified exchange client implementations under a common interface.
No classes or functions are defined here directly.
See the individual modules (binance.py, bingx.py, bitget.py, etc.)
for full concrete client implementations adhering to the ExchangeClient protocol.
"""
